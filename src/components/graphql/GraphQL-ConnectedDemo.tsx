const GraphQLConnectedDemo = (): JSX.Element => {
  // Query results in integrated GraphQL replace the normal `fields` data
  // i.e. with { data, }

  return (
    <div></div>
  );
};

export default GraphQLConnectedDemo;
